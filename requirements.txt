# --- Core 文档处理 ---
python-docx==1.1.0
pypdf==4.2.0
reportlab==4.2.0
pdfplumber==0.11.0

# --- 文本处理 / YAML 配置 / 正则辅助 ---
PyYAML==6.0.1
regex==2024.5.15

# --- Streamlit UI ---
streamlit==1.35.0

# --- 文件夹监听器 ---
watchdog==4.0.1

# --- Excel / CSV 处理 ---
pandas==2.2.2
openpyxl==3.1.3

# --- NLP（可选，但强烈推荐，为命名实体识别做准备） ---
spacy==3.7.2
spacy-transformers==1.3.4
# 多语言模型（可按需下载）
# python -m spacy download es_core_news_lg

# --- OCR（可选，用于扫描 PDF） ---
pytesseract==0.3.10
pdf2image==1.17.0

# --- PDF 转 Word 保留格式 ---
pdf2docx==0.5.8

# --- Utils ---
tqdm==4.66.4
rapidfuzz==3.6.1   # 用于模糊匹配客户名 / 机构名

# --- 如果未来要部署 API，可提前添加 ---
# fastapi==0.110.0
# uvicorn==0.29.0
